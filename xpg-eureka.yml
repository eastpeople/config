management:
  context-path: /admin
zuul:
  routes:
    xpg-sample-ge: /**
server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: true
management:
  security:
    enabled: false
endpoints:
  restart:
    enabled: true