server:
  port: 8761
eureka:
  dashboard:
    path: /eureka-dashboard
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: true
management:
  security:
    enabled: false
endpoints:
  restart:
    enabled: true