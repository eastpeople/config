server:
  port: 8761
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    preferIpAddress: true
management:
  security:
    enabled: false
endpoints:
  restart:
    enabled: true